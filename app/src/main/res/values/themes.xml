<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Assemble" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme3</item>
        <item name="colorPrimaryVariant">@color/theme2</item>
        <item name="colorOnPrimary">@color/theme</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:backgroundTint">@color/theme</item>
        <item name="android:fontFamily">@font/consolas</item>
        <item name="android:textColor">@color/theme2</item>
        <item name="android:textSize">25dp</item>
    </style>
</resources>